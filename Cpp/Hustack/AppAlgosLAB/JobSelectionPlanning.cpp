#include <bits/stdc++.h>

using namespace std;

const int N = 1e5 + 5;
int n;
pair<int, int> jobs[N];

void input(){
    ios_base::sync_with_stdio(0);
    cin.tie(NULL);
    cout.tie(NULL);
    cin >> n;
    for(int i = 1; i <= n; i++){
        cin >> jobs[i].first >> jobs[i].second;
    }
}

void solve(){

}

int main(){
    return 0;
}